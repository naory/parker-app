# ========================
# Parker App — Environment
# ========================
# Copy this to .env and fill in values

# --- White-Label Deployment ---
# Comma-separated ISO 3166-1 alpha-2 country codes for this deployment
# Controls: driver registration countries, ALPR plate formats, UI labels
# Examples: "IL" (single country), "US", "DE,FR,ES,IT,NL,GB" (EU region)
NEXT_PUBLIC_DEPLOYMENT_COUNTRIES=IL
DEPLOYMENT_COUNTRIES=IL

# --- Database ---
DATABASE_URL=postgresql://parker:parker@localhost:5432/parker

# --- API Server ---
PORT=3001
CORS_ORIGIN=http://localhost:3000,http://localhost:3002

# --- Blockchain: Base Sepolia (DriverRegistry + x402) ---
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
DEPLOYER_PRIVATE_KEY=
DRIVER_REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000

# --- Blockchain: Hedera (ParkingNFT via HTS) ---
# Create collection: pnpm --filter @parker/hedera setup
HEDERA_ACCOUNT_ID=0.0.xxxxx
HEDERA_PRIVATE_KEY=302e...
HEDERA_NETWORK=testnet
HEDERA_EVM_ADDRESS=
HEDERA_TOKEN_ID=

# --- x402 Crypto Payment Rail ---
X402_STABLECOIN=USDC
X402_NETWORK=base-sepolia
LOT_OPERATOR_WALLET=0x0000000000000000000000000000000000000001

# --- FX Rates (for converting local currency → stablecoin) ---
# Pattern: FX_RATE_{FROM}_{TO}=<rate>
# Only needed when lot currency differs from stablecoin base (USD for USDC)
FX_RATE_EUR_USD=1.08
FX_RATE_GBP_USD=1.27
# Add more as needed, e.g.: FX_RATE_JPY_USD=0.0067, FX_RATE_ILS_USD=0.274
# Override the stablecoin's base currency (default: inferred from token, e.g. USDC→USD)
# X402_BASE_CURRENCY=USD

# --- Stripe (Credit Card Payments) ---
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_SUCCESS_URL=http://localhost:3000/payment/success
STRIPE_CANCEL_URL=http://localhost:3000/payment/cancel

# --- Driver App ---
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
NEXT_PUBLIC_CHAIN_ID=84532

# --- Gate App ---
NEXT_PUBLIC_LOT_ID=LOT-001
NEXT_PUBLIC_WS_URL=ws://localhost:3001/ws

# --- Google Cloud (ALPR) ---
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
