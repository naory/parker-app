# Server
PORT=3001

# White-label: comma-separated ISO 3166-1 alpha-2 country codes for this deployment
# Controls ALPR plate format and driver registration validation
DEPLOYMENT_COUNTRIES=IL

# Database
DATABASE_URL=postgresql://parker:parker@localhost:5432/parker

# Blockchain — Base Sepolia (DriverRegistry)
RPC_URL=https://sepolia.base.org
DRIVER_REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000

# Blockchain — Hedera (ParkingNFT via HTS)
# Disabled if these are not set; create collection with: pnpm --filter @parker/hedera setup
HEDERA_ACCOUNT_ID=0.0.xxxxx
HEDERA_PRIVATE_KEY=302e...
HEDERA_NETWORK=testnet
HEDERA_TOKEN_ID=0.0.xxxxx

# NFT Metadata Encryption (AES-256-GCM)
# When set, on-chain NFT metadata is encrypted to prevent plate tracking.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NFT_ENCRYPTION_KEY=

# x402 Crypto Payment Rail
X402_STABLECOIN=USDC
X402_NETWORK=base-sepolia
LOT_OPERATOR_WALLET=0x0000000000000000000000000000000000000001
# For XRPL settlement adapter (required when X402_NETWORK starts with "xrpl:")
# XRPL testnet: wss://s.altnet.rippletest.net:51233
# XRPL mainnet: wss://xrplcluster.com
XRPL_RPC_URL=
# For Xaman native payload flow (recommended for XRPL UX)
XAMAN_API_URL=https://xumm.app
XAMAN_API_KEY=
XAMAN_API_SECRET=
# Required for non-XRP XRPL assets (e.g., RLUSD IOU)
XRPL_ISSUER=

# FX Rates (local currency → stablecoin base currency)
# Pattern: FX_RATE_{FROM}_{TO}=<rate>
FX_RATE_EUR_USD=1.08
FX_RATE_GBP_USD=1.27
# Add more as needed, e.g.: FX_RATE_JPY_USD=0.0067, FX_RATE_ILS_USD=0.274

# Stripe (Credit Card Payments) — disabled if not set
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_SUCCESS_URL=http://localhost:3000/payment/success
STRIPE_CANCEL_URL=http://localhost:3000/payment/cancel

# Google Cloud Vision (ALPR)
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3002
